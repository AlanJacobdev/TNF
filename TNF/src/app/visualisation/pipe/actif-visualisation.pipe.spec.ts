import { ActifVisualisationPipe } from './actif-visualisation.pipe';

describe('ActifVisualisationPipe', () => {
  it('create an instance', () => {
    const pipe = new ActifVisualisationPipe();
    expect(pipe).toBeTruthy();
  });
});
