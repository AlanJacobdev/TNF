import { FilterSuppressionPipe } from './filter-suppression.pipe';

describe('FilterSuppressionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSuppressionPipe();
    expect(pipe).toBeTruthy();
  });
});
