import { FilterDmdAdminPipe } from './filter-dmd-admin.pipe';

describe('FilterDmdAdminPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDmdAdminPipe();
    expect(pipe).toBeTruthy();
  });
});
