import { FilterActifPipe } from './filter-actif.pipe';

describe('FilterActifPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterActifPipe();
    expect(pipe).toBeTruthy();
  });
});
