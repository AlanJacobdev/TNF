import { FilterInfoPipe } from './filter-info.pipe';

describe('FilterInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
