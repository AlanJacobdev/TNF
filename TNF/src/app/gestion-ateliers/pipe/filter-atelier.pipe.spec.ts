import { FilterAtelierPipe } from './filter-atelier.pipe';

describe('FilterAtelierPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAtelierPipe();
    expect(pipe).toBeTruthy();
  });
});
