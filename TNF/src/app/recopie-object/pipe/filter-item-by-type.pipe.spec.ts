import { FilterItemByTypePipe } from './filter-item-by-type.pipe';

describe('FilterItemByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItemByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
