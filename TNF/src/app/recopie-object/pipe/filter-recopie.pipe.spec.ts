import { FilterRecopiePipe } from './filter-recopie.pipe';

describe('FilterRecopiePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRecopiePipe();
    expect(pipe).toBeTruthy();
  });
});
