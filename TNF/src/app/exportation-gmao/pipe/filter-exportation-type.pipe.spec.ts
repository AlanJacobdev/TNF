import { FilterExportationTypePipe } from './filter-exportation-type.pipe';

describe('FilterExportationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExportationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
