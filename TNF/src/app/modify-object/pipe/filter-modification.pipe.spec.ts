import { FilterModificationPipe } from './filter-modification.pipe';

describe('FilterModificationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterModificationPipe();
    expect(pipe).toBeTruthy();
  });
});
